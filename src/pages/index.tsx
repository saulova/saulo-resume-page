// node_modules
import type { NextPage } from "next";

// components
import Home from "@src-path/components/Pages/Home";

const HomePage: NextPage = () => {
  return <Home />;
};

export default HomePage;
